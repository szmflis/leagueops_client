import React from 'react'
import styled from 'styled-components'

const StyledTraitsContainer = styled.div`
  background: white;
`

const TraitsBox = () => {
  return (
    <StyledTraitsContainer>
      Test
    </StyledTraitsContainer>
  )
}

export default TraitsBox
